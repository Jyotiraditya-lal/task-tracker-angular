import { PriorityShadePipe } from './priority-shade.pipe';

describe('PriorityShadePipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityShadePipe();
    expect(pipe).toBeTruthy();
  });
});
